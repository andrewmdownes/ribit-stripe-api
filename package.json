{
  "name": "ribit-stripe-api",
  "version": "1.0.0",
  "description": "Ribit Stripe Payment API for Vercel",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "stripe": "^18.2.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.x"
  }
}